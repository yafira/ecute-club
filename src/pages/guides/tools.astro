---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Note from "../../components/Note.astro";
const title = "tools — ecute.club";
---
<BaseLayout {title} bodyClass="bg-lined">
  <article class="container">
    <h1>tools</h1>

    <div class="diagram" aria-label="iron + DMM + cutters icons">
      <svg viewBox="0 0 720 140" width="100%" height="auto">
        <rect x="10" y="10" width="700" height="120" rx="16" fill="var(--card)"/>
        <!-- iron -->
        <text x="80" y="34" class="mono">iron</text>
        <rect x="40" y="50" width="80" height="20" rx="10" fill="none" stroke="currentColor" stroke-width="3"/>
        <polygon points="120,50 140,60 120,70" fill="none" stroke="currentColor" stroke-width="3"/>
        <!-- dmm -->
        <text x="300" y="34" class="mono">DMM</text>
        <rect x="250" y="44" width="110" height="70" rx="10" fill="none" stroke="currentColor" stroke-width="3"/>
        <circle cx="305" cy="64" r="12" fill="none" stroke="currentColor" stroke-width="2"/>
        <rect x="265" y="86" width="30" height="16" rx="4" fill="none" stroke="currentColor" stroke-width="2"/>
        <rect x="305" y="86" width="30" height="16" rx="4" fill="none" stroke="currentColor" stroke-width="2"/>
        <!-- cutters -->
        <text x="540" y="34" class="mono">cutters</text>
        <path d="M520,80 C540,40 560,40 580,80" fill="none" stroke="currentColor" stroke-width="3"/>
        <path d="M520,80 C540,120 560,120 580,80" fill="none" stroke="currentColor" stroke-width="3"/>
      </svg>
    </div>

    <h2>bench basics</h2>
    <ul>
      <li>soldering iron (330–360 °C leaded, 360–380 °C lead-free), brass wool/sponge, 63/37 solder, flux pen</li>
      <li>cutters, needle-nose pliers, tweezers, wire strippers, helping hands</li>
      <li>DMM (continuity, voltage, current), optional logic probe/USB logic analyzer</li>
      <li>heat-shrink, hot glue, electrical tape, zip ties</li>
    </ul>

    <Note variant="try">practice drag-soldering on a scrap board; flux is 90% of the trick.</Note>
    <Note variant="care">keep tips tinned; never scrape on copper pads — use wick + flux.</Note>
  </article>
</BaseLayout>
